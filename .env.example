# Server Environment Variables
# Supabase Postgres connection strings
DATABASE_URL="postgres://USERNAME:PASSWORD@db.supabase.co:6543/postgres"
# Optional: direct connection string for Drizzle migrations (non-pooler)
DIRECT_URL="postgres://USERNAME:PASSWORD@db.supabase.co:5432/postgres"

# NextAuth configuration
NEXTAUTH_SECRET="generate_a_secure_random_value"
# Update to your deployed URL when not running locally
NEXTAUTH_URL="http://localhost:3000"

# Supabase project credentials
SUPABASE_URL="https://YOUR-PROJECT.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="supabase_service_role_key"
# Bucket used for uploads; defaults to "uploads" if omitted
SUPABASE_BUCKET="uploads"

# Secret shared with the revalidation API endpoint
REVALIDATE_SECRET="choose_a_strong_secret"

# Optional owner seed values consumed by scripts/seed.ts
OWNER_EMAIL="owner@example.com"
# Bcrypt hash for the owner password; generate via your preferred tooling
OWNER_PASSWORD_HASH="$2b$12$examplehash"

# Client-side Environment Variables
# Expose the Supabase URL to the browser for storage previews and Next/Image patterns
NEXT_PUBLIC_SUPABASE_URL="https://YOUR-PROJECT.supabase.co"
# Canonical site URL used for metadata generation
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
